# Launch — project instructions

## Versioning
- Current version lives in `version.go` (`const Version`).
- Update `CHANGELOG.md` with a new entry each session where user-facing changes are made.
- If the session introduces changes that warrant a version bump, ask the user which version to use before writing it.
